"""Точка входа для команды `python -m double_well_solver`.

Вся логика командной строки находится в `cli.main`; здесь остаётся только её
вызов. Такой маленький модуль — стандартный способ сделать Python-пакет
запускаемым через ключ `-m`.
"""

from .cli import main


if __name__ == "__main__":
    main()
